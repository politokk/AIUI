---
title: Components
description: All available components from Fumadocs
---

This page showcases all the components available in your Fumadocs installation.

## Banner

The Banner component displays important announcements or messages at the top of your site.

### Default Banner

<div className="not-prose">
  <Banner changeLayout={false}>
    This is an important announcement! Check out our new features.
  </Banner>
</div>

### Rainbow Variant

<div className="not-prose mt-4">
  <Banner variant="rainbow" changeLayout={false}>
    🎉 Exciting news! New components are now available.
  </Banner>
</div>

### Custom Rainbow Colors

<div className="not-prose mt-4">
  <Banner 
    variant="rainbow" 
    changeLayout={false}
    rainbowColors={[
      'hsla(154, 100%, 76%, 1)',
      'hsla(234, 100%, 83%, 1)',
      'transparent',
      'hsla(288, 100%, 81%, 1)',
      'transparent',
      'hsla(154, 100%, 76%, 1)',
      'transparent',
    ]}
  >
    🔥 Hot update: Custom rainbow colors!
  </Banner>
</div>

### Closable Banner

Give the banner an ID to make it closable. The state will be automatically persisted.

<div className="not-prose mt-4">
  <Banner id="demo-banner" changeLayout={false}>
    ✨ This banner can be closed! Click the X button →
  </Banner>
</div>

### Usage in Layout

To use the banner in your root layout for site-wide announcements:

```tsx
import { Banner } from 'fumadocs-ui/components/banner';

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body>
        <Banner id="announcement">
          🚀 New documentation site launched!
        </Banner>
        {children}
      </body>
    </html>
  );
}
```

**Note:** Set `changeLayout={false}` when displaying banners inline in documentation to prevent layout modifications.

## Callout

Callouts are used to highlight important information.

<Callout>
  This is a default callout with important information.
</Callout>

<Callout type="info">
  This is an info callout providing additional context.
</Callout>

<Callout type="warning">
  This is a warning callout for important notices.
</Callout>

<Callout type="error">
  This is an error callout for critical information.
</Callout>

## Cards

Cards are great for displaying grouped content with links.

<Cards>
  <Card title="Next.js Documentation" href="https://nextjs.org/docs">
    Learn more about Next.js features and API.
  </Card>
  <Card title="Fumadocs Guide" href="https://fumadocs.vercel.app">
    Explore the complete Fumadocs documentation.
  </Card>
  <Card title="React Documentation" href="https://react.dev">
    Official React documentation and tutorials.
  </Card>
</Cards>

## Accordion

Accordions help organize content in collapsible sections.

<Accordions>
  <Accordion title="What is Fumadocs?">
    Fumadocs is a documentation framework built on Next.js, designed to create beautiful documentation sites with minimal effort.
  </Accordion>
  <Accordion title="How do I install components?">
    You can install components using the Fumadocs CLI with the command `pnpm dlx @fumadocs/cli add`.
  </Accordion>
  <Accordion title="Can I customize the components?">
    Yes! All components are fully customizable. You can modify their styles and behavior to match your needs.
  </Accordion>
</Accordions>

## Code Block

The CodeBlock component provides enhanced code display with syntax highlighting.

<CodeBlock lang="typescript" title="example.ts">
{`interface User {
  id: string;
  name: string;
  email: string;
}

function greetUser(user: User): string {
  return \`Hello, \${user.name}!\`;
}`}
</CodeBlock>

## Files

Display file and folder structures clearly.

<Files>
  <Folder name="src" defaultOpen>
    <Folder name="components">
      <File name="Button.tsx" />
      <File name="Card.tsx" />
      <File name="Dialog.tsx" />
    </Folder>
    <Folder name="lib">
      <File name="utils.ts" />
      <File name="constants.ts" />
    </Folder>
    <File name="app.tsx" />
  </Folder>
  <File name="package.json" />
  <File name="tsconfig.json" />
</Files>

## Steps

Steps are perfect for tutorials and guides.

<Steps>
  <Step>
    ### Install Dependencies
    
    First, install the required packages:
    
    ```bash
    pnpm install fumadocs-ui fumadocs-core
    ```
  </Step>
  
  <Step>
    ### Configure Your Project
    
    Create a `source.config.ts` file in your project root:
    
    ```typescript
    import { defineConfig } from 'fumadocs-core/config';
    
    export default defineConfig({
      // Your configuration
    });
    ```
  </Step>
  
  <Step>
    ### Start Building
    
    You're all set! Start creating your documentation:
    
    ```bash
    pnpm dev
    ```
  </Step>
</Steps>

## Tabs

Organize content in tabbed interfaces.

<Tabs defaultValue="npm">
  <Tab value="npm" label="npm">
    ```bash
    npm install fumadocs-ui
    ```
  </Tab>
  <Tab value="pnpm" label="pnpm">
    ```bash
    pnpm add fumadocs-ui
    ```
  </Tab>
  <Tab value="yarn" label="yarn">
    ```bash
    yarn add fumadocs-ui
    ```
  </Tab>
  <Tab value="bun" label="bun">
    ```bash
    bun add fumadocs-ui
    ```
  </Tab>
</Tabs>

## Type Table

Display TypeScript types and properties in a structured table.

<TypeTable
  type={{
    title: {
      type: 'string',
      description: 'The title of the component',
      required: true,
    },
    description: {
      type: 'string',
      description: 'Optional description text',
      required: false,
    },
    variant: {
      type: "'default' | 'primary' | 'secondary'",
      description: 'The visual variant of the component',
      default: 'default',
    },
    size: {
      type: "'sm' | 'md' | 'lg'",
      description: 'The size of the component',
      default: 'md',
    },
  }}
/>

## Image Zoom

Enable zooming on images for better detail viewing.

<ImageZoom
  src="https://images.unsplash.com/photo-1555066931-4365d14bab8c?w=800"
  alt="Code on a laptop screen"
  width={800}
  height={600}
/>

## Inline Table of Contents

Display a table of contents inline within your content.

<InlineTOC 
  items={[
    { title: "Introduction", url: "#introduction", depth: 1 },
    { title: "Getting Started", url: "#getting-started", depth: 2 },
    { title: "Installation", url: "#installation", depth: 3 },
    { title: "Configuration", url: "#configuration", depth: 3 },
    { title: "Advanced Usage", url: "#advanced-usage", depth: 2 },
    { title: "API Reference", url: "#api-reference", depth: 2 }
  ]}
/>

## GitHub Info

Display repository information from GitHub.

<GithubInfo
  owner="fuma-nama"
  repo="fumadocs"
/>

## Standard Code Blocks

Regular code blocks with syntax highlighting:

```javascript
// JavaScript example
const greet = (name) => {
  console.log(`Hello, ${name}!`);
};

greet('World');
```

```python
# Python example
def fibonacci(n):
    if n <= 1:
        return n
    return fibonacci(n-1) + fibonacci(n-2)

print(fibonacci(10))
```

```css
/* CSS example */
.container {
  display: flex;
  justify-content: center;
  align-items: center;
  min-height: 100vh;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
}
```

## Combining Components

You can combine multiple components for rich documentation:

<Callout type="info">
  The following example shows how to use multiple components together.
</Callout>

<Tabs defaultValue="setup">
  <Tab value="setup" label="Setup">
    <Steps>
      <Step>
        Install the required dependencies:
        ```bash
        pnpm add fumadocs-ui
        ```
      </Step>
      <Step>
        Import the components you need:
        ```tsx
        import { Card, Cards } from '@/components/card';
        ```
      </Step>
    </Steps>
  </Tab>
  <Tab value="usage" label="Usage">
    ```tsx
    export function MyComponent() {
      return (
        <Cards>
          <Card title="Example Card">
            This is how you use the Card component.
          </Card>
        </Cards>
      );
    }
    ```
  </Tab>
</Tabs>

That's all the components available in your Fumadocs installation! Each component is designed to enhance your documentation with interactive and visually appealing elements.
